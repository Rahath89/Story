git init
git status

working directory   Staging Area   Local Repository   Remote Repository

git add . or"filename"  //adding files to staging area
git rm --cached . -r  //removing files from staging area
git commit -m "Change chapter 1" // commit massage
git log // to see the time and author who does the change/changes
git diff chapter3.txt // to chacke the current file content and last save file content
git checkout  filename // to revert back the last save of the content


//pushing local repo to github
git remote add origin https://github.com/Rahath89/Story.git
git push -u origin master

//gitignore
.gitignore  //to create gitignore file for hiding files to be committed need to write them inside gitignore file
github/gitignore  // swift/gitignore code for avoiding specfic files so that it wont colide and push to public github

//gitclone
git clone https://github.com/austinzheng/swift-2048.git

//branching and merging
git branch "create a new branch"
git branch  // will display any branch include master branch and *master will be current branch
git checkout "new branch"  / will switch to new branch